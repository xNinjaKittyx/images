FROM rust:1.92 AS base
RUN cargo install cargo-chef sccache --locked
ENV RUSTC_WRAPPER=sccache SCCACHE_DIR=/sccache  
RUN apt update && apt upgrade -y && apt install -y curl cmake && apt clean && rm -rf /var/lib/apt/lists/*
